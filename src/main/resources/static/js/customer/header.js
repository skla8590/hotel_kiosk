/* 홈 버튼 (메인화면 돌아가기) */
function goHome() {
    window.location.href = '/JHotel/main';
}

/* 이전으로 돌아가기 */
function goBack() {
    window.history.back();
}

/* 호텔 안내 버튼 */
function goInfo() {
    window.location.href = '/JHotel/hotel_info';
}